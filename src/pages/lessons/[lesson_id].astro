---
import Layout from '../../layouts/Layout.astro';
import { lessons } from '../../data/lingo-lessons-data';
import type { Lesson } from '../../types/lesson';

export const prerender = true;

export interface Props {
  lesson_id: string;
  lesson: Lesson;
}

export async function getStaticPaths() {
  return lessons.map((lesson) => ({
    params: { lesson_id: lesson.lesson_id },
    props: { lesson }
  }));
}

const { lesson } = Astro.props;
---

<Layout title={`${lesson.title} - Speed of Mastery`}>
  <!-- Navigation Header -->
  <header class="bg-white shadow-sm border-b">
    <div class="container mx-auto px-4 py-4">
      <div class="flex items-center justify-between">
        <a href="/" class="text-2xl font-bold text-primary">TRM2 - Arabic Learning</a>
        <nav class="flex space-x-6">
          <a href="/" class="text-gray-700 hover:text-primary">Home</a>
          <a href="/lessons" class="text-gray-700 hover:text-primary">Lessons</a>
          <a href="/ai" class="text-gray-700 hover:text-primary">AI Tools</a>
          <a href="/progress" class="text-gray-700 hover:text-primary">Progress</a>
        </nav>
      </div>
    </div>
  </header>

  <!-- Main Content -->
  <div class="container mx-auto max-w-4xl py-8 px-4">
    <!-- Back Button -->
    <div class="mb-6">
      <a href="/" class="inline-flex items-center px-4 py-2 border border-gray-300 rounded-md shadow-sm text-sm font-medium text-gray-700 bg-white hover:bg-gray-50">
        <svg class="mr-2 h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"></path>
        </svg>
        Back to Home
      </a>
    </div>

    <!-- Lesson Header -->
    <div class="mb-8 text-center">
      <h1 class="text-4xl font-bold text-primary mb-2">{lesson.title}</h1>
      <div class="flex items-center justify-center space-x-2">
        <div class="inline-flex items-center rounded-full border px-2.5 py-0.5 text-xs font-semibold transition-colors focus:outline-none focus:ring-2 focus:ring-ring focus:ring-offset-2 border-transparent bg-secondary text-secondary-foreground hover:bg-secondary/80">
          {lesson.topic}
        </div>
        <div class="inline-flex items-center rounded-full border px-2.5 py-0.5 text-xs font-semibold transition-colors focus:outline-none focus:ring-2 focus:ring-ring focus:ring-offset-2 border-transparent hover:bg-primary/80 bg-accent text-accent-foreground">
          {lesson.level}
        </div>
      </div>
    </div>

    <!-- AI Tools Section -->
    <div class="rounded-lg border bg-card text-card-foreground shadow-sm mb-8">
      <div class="flex flex-col space-y-1.5 p-6">
        <div class="text-2xl font-semibold leading-none tracking-tight flex items-center gap-2">
          <svg class="h-5 w-5 text-primary" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.5 2A2.5 2.5 0 0 1 12 4.5v15a2.5 2.5 0 0 1-4.96.44 2.5 2.5 0 0 1-2.96-3.08 3 3 0 0 1-.34-5.58 2.5 2.5 0 0 1 1.32-4.24 2.5 2.5 0 0 1 1.98-3A2.5 2.5 0 0 1 9.5 2Z"></path>
            <path d="M14.5 2A2.5 2.5 0 0 0 12 4.5v15a2.5 2.5 0 0 0 4.96.44 2.5 2.5 0 0 0 2.96-3.08 3 3 0 0 0 .34-5.58 2.5 2.5 0 0 0-1.32-4.24 2.5 2.5 0 0 0-1.98-3A2.5 2.5 0 0 0 14.5 2Z"></path>
          </svg>
          أدوات الذكاء الاصطناعي للدرس
        </div>
        <p class="text-sm text-muted-foreground">
          استخدم الذكاء الاصطناعي لتعزيز فهمك لهذا الدرس
          <span class="text-yellow-600 ml-2">(جاري تهيئة النظام...)</span>
        </p>
      </div>
      <div class="p-6 pt-0">
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
          <button class="justify-center whitespace-nowrap rounded-md text-sm font-medium ring-offset-background transition-colors focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 disabled:pointer-events-none disabled:opacity-50 border border-input bg-background hover:bg-accent hover:text-accent-foreground h-auto p-4 flex flex-col items-center gap-2" onclick="openDialog('tts')">
            <svg class="h-6 w-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <polygon points="11 5 6 9 2 9 2 15 6 15 11 19 11 5"></polygon>
              <path d="M15.54 8.46a5 5 0 0 1 0 7.07"></path>
              <path d="M19.07 4.93a10 10 0 0 1 0 14.14"></path>
            </svg>
            <span>استماع للنص</span>
          </button>
          
          <button class="justify-center whitespace-nowrap rounded-md text-sm font-medium ring-offset-background transition-colors focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 disabled:pointer-events-none disabled:opacity-50 border border-input bg-background hover:bg-accent hover:text-accent-foreground h-auto p-4 flex flex-col items-center gap-2" onclick="openDialog('stt')">
            <svg class="h-6 w-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path d="M12 2a3 3 0 0 0-3 3v7a3 3 0 0 0 6 0V5a3 3 0 0 0-3-3Z"></path>
              <path d="M19 10v2a7 7 0 0 1-14 0v-2"></path>
              <line x1="12" x2="12" y1="19" y2="22"></line>
            </svg>
            <span>التحدث للنص</span>
          </button>
          
          <button class="justify-center whitespace-nowrap rounded-md text-sm font-medium ring-offset-background transition-colors focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 disabled:pointer-events-none disabled:opacity-50 border border-input bg-background hover:bg-accent hover:text-accent-foreground h-auto p-4 flex flex-col items-center gap-2" onclick="openDialog('ai-question')" disabled>
            <svg class="h-6 w-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"></path>
              <line x1="9" x2="15" y1="10" y2="10"></line>
              <line x1="12" x2="12" y1="7" y2="13"></line>
            </svg>
            <span>اسأل الذكاء الاصطناعي</span>
          </button>
          
          <button class="justify-center whitespace-nowrap rounded-md text-sm font-medium ring-offset-background transition-colors focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 disabled:pointer-events-none disabled:opacity-50 border border-input bg-background hover:bg-accent hover:text-accent-foreground h-auto p-4 flex flex-col items-center gap-2" onclick="openDialog('rag')" disabled>
            <svg class="h-6 w-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path d="M2 3h6a4 4 0 0 1 4 4v14a3 3 0 0 0-3-3H2z"></path>
              <path d="M22 3h-6a4 4 0 0 0-4 4v14a3 3 0 0 1 3-3h7z"></path>
            </svg>
            <span>قاعدة المعرفة</span>
          </button>
          
          <button class="justify-center whitespace-nowrap rounded-md text-sm font-medium ring-offset-background transition-colors focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 disabled:pointer-events-none disabled:opacity-50 border border-input bg-background hover:bg-accent hover:text-accent-foreground h-auto p-4 flex flex-col items-center gap-2" onclick="openDialog('translation')" disabled>
            <svg class="h-6 w-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path d="m5 8 6 6"></path>
              <path d="m4 14 6-6 2-3"></path>
              <path d="M2 5h12"></path>
              <path d="M7 2h1"></path>
              <path d="m22 22-5-10-5 10"></path>
              <path d="M14 18h6"></path>
            </svg>
            <span>ترجمة ذكية</span>
          </button>
          
          <button class="justify-center whitespace-nowrap rounded-md text-sm font-medium ring-offset-background transition-colors focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 disabled:pointer-events-none disabled:opacity-50 border border-input bg-background hover:bg-accent hover:text-accent-foreground h-auto p-4 flex flex-col items-center gap-2" onclick="openDialog('summary')" disabled>
            <svg class="h-6 w-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path d="m12 3-1.912 5.813a2 2 0 0 1-1.275 1.275L3 12l5.813 1.912a2 2 0 0 1 1.275 1.275L12 21l1.912-5.813a2 2 0 0 1 1.275-1.275L21 12l-5.813-1.912a2 2 0 0 1-1.275-1.275L12 3Z"></path>
              <path d="M5 3v4"></path>
              <path d="M19 17v4"></path>
              <path d="M3 5h4"></path>
              <path d="M17 19h4"></path>
            </svg>
            <span>ملخص الدرس</span>
          </button>
        </div>
      </div>
    </div>

    <!-- Lesson Content -->
    <div class="rounded-lg border bg-card text-card-foreground mb-6 shadow-lg">
      <div class="flex flex-col space-y-1.5 p-6">
        <div class="font-semibold tracking-tight flex items-center text-2xl">
          <svg class="mr-2 h-6 w-6 text-primary" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path d="M2 3h6a4 4 0 0 1 4 4v14a3 3 0 0 0-3-3H2z"></path>
            <path d="M22 3h-6a4 4 0 0 0-4 4v14a3 3 0 0 1 3-3h7z"></path>
          </svg>
          Explanation (الشرح)
        </div>
      </div>
      <div class="p-6 pt-0">
        <p class="text-lg leading-relaxed whitespace-pre-line text-right" dir="rtl">{lesson.arabic_explanation}</p>
      </div>
    </div>

    <!-- Interactive Exercises -->
    {lesson.interactive_exercises && lesson.interactive_exercises.length > 0 && (
      <div class="rounded-lg border bg-card text-card-foreground mb-6 shadow-lg">
        <div class="flex flex-col space-y-1.5 p-6">
          <div class="font-semibold tracking-tight flex items-center text-2xl">
            <svg class="mr-2 h-6 w-6 text-primary" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path d="m3 21 1.9-5.7a8.5 8.5 0 1 1 3.8 3.8z"></path>
            </svg>
            Interactive Exercises (تمارين تفاعلية)
          </div>
        </div>
        <div class="p-6 pt-0">
          {lesson.interactive_exercises.map((exercise, index) => (
            <div class="rounded-lg border bg-card text-card-foreground mb-4 shadow-md">
              <div class="flex flex-col space-y-1.5 p-6">
                <div class="font-semibold tracking-tight text-xl">{exercise.question}</div>
              </div>
              <div class="p-6 pt-0">
                <div role="radiogroup" aria-required="false" dir="ltr" class="grid gap-2 space-y-3" tabindex="-1" style="outline:none">
                  {exercise.choices.map((choice, choiceIndex) => (
                    <div class="flex items-center space-x-2">
                      <button type="button" role="radio" aria-checked="false" data-state="unchecked" value={choice} class="aspect-square h-4 w-4 rounded-full border border-primary text-primary ring-offset-background focus:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 disabled:cursor-not-allowed disabled:opacity-50" id={`${exercise.question}-choice-${choiceIndex}`} tabindex="-1" data-radix-collection-item=""></button>
                      <input type="radio" aria-hidden="true" style="transform:translateX(-100%);position:absolute;pointer-events:none;opacity:0;margin:0" tabindex="-1" value={choice}/>
                      <label class="font-medium peer-disabled:cursor-not-allowed peer-disabled:opacity-70 text-md cursor-pointer" for={`${exercise.question}-choice-${choiceIndex}`}>{choice}</label>
                    </div>
                  ))}
                </div>
              </div>
              <div class="p-6 pt-0 flex flex-col items-start space-y-4">
                <button class="inline-flex items-center justify-center whitespace-nowrap rounded-md text-sm font-medium ring-offset-background transition-colors focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 disabled:pointer-events-none disabled:opacity-50 bg-primary text-primary-foreground hover:bg-primary/90 h-10 px-4 py-2 w-full sm:w-auto" onclick="checkAnswer(this, '${exercise.correct_answer}')">Check Answer</button>
              </div>
            </div>
          ))}
        </div>
      </div>
    )}

    <!-- Additional Notes -->
    {lesson.additional_notes && (
      <div class="rounded-lg border bg-card text-card-foreground mb-6 shadow-lg border-primary/50">
        <div class="flex flex-col space-y-1.5 p-6">
          <h3 class="font-semibold tracking-tight flex items-center text-2xl text-primary">
            <svg class="mr-2 h-6 w-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path d="M15.5 3H5a2 2 0 0 0-2 2v14c0 1.1.9 2 2 2h14a2 2 0 0 0 2-2V8.5L15.5 3Z"></path>
              <path d="M15 3v6h6"></path>
            </svg>
            Additional Notes (ملاحظات إضافية)
          </h3>
        </div>
        <div class="p-6 pt-0">
          <div class="w-full" data-orientation="vertical">
            <div data-state="open" data-orientation="vertical" class="border-b">
              <h3 data-orientation="vertical" data-state="open" class="flex">
                <button type="button" aria-controls="radix-:r1R366:" aria-expanded="true" data-state="open" data-orientation="vertical" id="radix-:r1R166:" class="flex flex-1 items-center justify-between py-4 font-medium transition-all [&amp;[data-state=open]&gt;svg]:rotate-180 text-lg hover:no-underline" data-radix-collection-item="">Toggle Notes<svg class="h-4 w-4 shrink-0 transition-transform duration-200" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path d="m6 9 6 6 6-6"></path>
                </svg></button>
              </h3>
              <div data-state="open" id="radix-:r1R366:" role="region" aria-labelledby="radix-:r1R166:" data-orientation="vertical" class="overflow-hidden text-sm transition-all data-[state=closed]:animate-accordion-up data-[state=open]:animate-accordion-down" style="--radix-accordion-content-height:var(--radix-collapsible-content-height);--radix-accordion-content-width:var(--radix-collapsible-content-width)">
                <div class="pb-4 pt-0">
                  <div class="mb-4">
                    <p class="text-md leading-relaxed whitespace-pre-line">{lesson.additional_notes}</p>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    )}

    <!-- Common Mistakes -->
    {lesson.common_mistakes && (
      <div class="rounded-lg border bg-card text-card-foreground mb-6 shadow-lg">
        <div class="flex flex-col space-y-1.5 p-6">
          <h3 class="font-semibold tracking-tight flex items-center text-2xl">
            <svg class="mr-2 h-6 w-6 text-destructive" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path d="m21.73 18-8-14a2 2 0 0 0-3.48 0l-8 14A2 2 0 0 0 4 21h16a2 2 0 0 0 1.73-3Z"></path>
              <path d="M12 9v4"></path>
              <path d="M12 17h.01"></path>
            </svg>
            Common Mistakes (أخطاء شائعة)
          </h3>
        </div>
        <div class="p-6 pt-0">
          <div class="w-full" data-orientation="vertical">
            <div data-state="open" data-orientation="vertical" class="border-b">
              <h3 data-orientation="vertical" data-state="open" class="flex">
                <button type="button" aria-controls="radix-:r1R376:" aria-expanded="true" data-state="open" data-orientation="vertical" id="radix-:r1R176:" class="flex flex-1 items-center justify-between py-4 font-medium transition-all [&amp;[data-state=open]&gt;svg]:rotate-180 text-lg hover:no-underline" data-radix-collection-item="">Toggle Mistakes<svg class="h-4 w-4 shrink-0 transition-transform duration-200" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path d="m6 9 6 6 6-6"></path>
                </svg></button>
              </h3>
              <div data-state="open" id="radix-:r1R376:" role="region" aria-labelledby="radix-:r1R176:" data-orientation="vertical" class="overflow-hidden text-sm transition-all data-[state=closed]:animate-accordion-up data-[state=open]:animate-accordion-down" style="--radix-accordion-content-height:var(--radix-collapsible-content-height);--radix-accordion-content-width:var(--radix-collapsible-content-width)">
                <div class="pb-4 pt-0">
                  <div class="mb-4">
                    <p class="text-md leading-relaxed whitespace-pre-line">{lesson.common_mistakes}</p>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    )}

    <!-- AI Tutor Button -->
    <button class="inline-flex items-center justify-center whitespace-nowrap text-sm font-medium ring-offset-background transition-colors focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 disabled:pointer-events-none disabled:opacity-50 bg-primary text-primary-foreground hover:bg-primary/90 fixed bottom-6 right-6 h-14 w-14 rounded-full shadow-xl z-50 p-0" aria-label="Open AI Lesson Tutor" onclick="openDialog('tutor')">
      <svg class="h-7 w-7" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"></path>
        <line x1="9" x2="15" y1="10" y2="10"></line>
        <line x1="12" x2="12" y1="7" y2="13"></line>
      </svg>
    </button>
  </div>

  <!-- AI Function Dialogs -->
  
  <!-- TTS Dialog -->
  <div id="tts-dialog" class="fixed inset-0 bg-black bg-opacity-50 hidden z-50">
    <div class="flex items-center justify-center min-h-screen p-4">
      <div class="bg-white rounded-lg max-w-2xl w-full max-h-[80vh] overflow-y-auto">
        <div class="flex items-center justify-between p-6 border-b">
          <h2 class="text-xl font-semibold">استماع للنص</h2>
          <button onclick="closeDialog('tts')" class="text-gray-400 hover:text-gray-600">
            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
            </svg>
          </button>
        </div>
        <div class="p-6">
          <p class="text-gray-600 mb-4">اختر النص الذي تريد الاستماع إليه</p>
          <div class="space-y-3">
            <button class="w-full text-left p-3 border rounded hover:bg-gray-50" onclick="speakText('{lesson.title}', 'en')">
              <span class="mr-2">▶️</span>
              عنوان الدرس: {lesson.title}
            </button>
            <button class="w-full text-left p-3 border rounded hover:bg-gray-50" onclick="speakText('{lesson.arabic_explanation || lesson.title}', 'ar')">
              <span class="mr-2">▶️</span>
              الشرح بالعربية
            </button>
            {lesson.examples && lesson.examples.length > 0 && lesson.examples.map((example, index) => (
              <button class="w-full text-left p-3 border rounded hover:bg-gray-50" onclick="speakText('{example.english}', 'en')">
                <span class="mr-2">▶️</span>
                مثال {index + 1}: {example.english}
              </button>
            ))}
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- STT Dialog -->
  <div id="stt-dialog" class="fixed inset-0 bg-black bg-opacity-50 hidden z-50">
    <div class="flex items-center justify-center min-h-screen p-4">
      <div class="bg-white rounded-lg max-w-2xl w-full">
        <div class="flex items-center justify-between p-6 border-b">
          <h2 class="text-xl font-semibold">التحدث للنص</h2>
          <button onclick="closeDialog('stt')" class="text-gray-400 hover:text-gray-600">
            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
            </svg>
          </button>
        </div>
        <div class="p-6">
          <p class="text-gray-600 mb-4">تحدث وسأقوم بتحويل كلامك إلى نص</p>
          <button id="stt-button" class="w-full p-3 bg-blue-600 text-white rounded hover:bg-blue-700" onclick="startListening()">
            <span class="mr-2">🎤</span>
            ابدأ التحدث
          </button>
          <div id="transcribed-text" class="mt-4 p-4 bg-gray-100 rounded hidden">
            <h4 class="font-semibold mb-2">النص المحول:</h4>
            <p id="stt-result"></p>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- AI Question Dialog -->
  <div id="ai-question-dialog" class="fixed inset-0 bg-black bg-opacity-50 hidden z-50">
    <div class="flex items-center justify-center min-h-screen p-4">
      <div class="bg-white rounded-lg max-w-2xl w-full">
        <div class="flex items-center justify-between p-6 border-b">
          <h2 class="text-xl font-semibold">اسأل الذكاء الاصطناعي</h2>
          <button onclick="closeDialog('ai-question')" class="text-gray-400 hover:text-gray-600">
            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
            </svg>
          </button>
        </div>
        <div class="p-6">
          <p class="text-gray-600 mb-4">اطرح سؤالاً حول هذا الدرس وسأجيبك باستخدام قاعدة المعرفة</p>
          <textarea id="ai-question-input" placeholder="اكتب سؤالك هنا..." rows="3" class="w-full p-3 border rounded mb-4"></textarea>
          <button id="ai-ask-button" class="w-full p-3 bg-blue-600 text-white rounded hover:bg-blue-700" onclick="askAI()">
            اسأل الذكاء الاصطناعي
          </button>
          <div id="ai-response" class="mt-4 p-4 bg-gray-100 rounded hidden">
            <h4 class="font-semibold mb-2">إجابة الذكاء الاصطناعي:</h4>
            <p id="ai-response-text"></p>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- RAG Knowledge Dialog -->
  <div id="rag-dialog" class="fixed inset-0 bg-black bg-opacity-50 hidden z-50">
    <div class="flex items-center justify-center min-h-screen p-4">
      <div class="bg-white rounded-lg max-w-4xl w-full max-h-[80vh] overflow-y-auto">
        <div class="flex items-center justify-between p-6 border-b">
          <h2 class="text-xl font-semibold">قاعدة المعرفة للدرس</h2>
          <button onclick="closeDialog('rag')" class="text-gray-400 hover:text-gray-600">
            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
            </svg>
          </button>
        </div>
        <div class="p-6">
          <p class="text-gray-600 mb-4">ابحث في المعرفة المخزنة من هذا الدرس</p>
          <div class="flex gap-2 mb-4">
            <input id="rag-search-input" placeholder="ابحث في المعرفة..." class="flex-1 p-3 border rounded">
            <button onclick="searchRAG()" class="px-4 py-3 bg-blue-600 text-white rounded hover:bg-blue-700">بحث</button>
          </div>
          <div id="rag-results" class="hidden">
            <div class="border rounded p-4">
              <h3 class="font-semibold mb-2">نتائج البحث</h3>
              <div id="rag-results-content" class="space-y-3 max-h-64 overflow-y-auto"></div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Translation Dialog -->
  <div id="translation-dialog" class="fixed inset-0 bg-black bg-opacity-50 hidden z-50">
    <div class="flex items-center justify-center min-h-screen p-4">
      <div class="bg-white rounded-lg max-w-2xl w-full">
        <div class="flex items-center justify-between p-6 border-b">
          <h2 class="text-xl font-semibold">ترجمة ذكية</h2>
          <button onclick="closeDialog('translation')" class="text-gray-400 hover:text-gray-600">
            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
            </svg>
          </button>
        </div>
        <div class="p-6">
          <p class="text-gray-600 mb-4">ترجم النصوص بين العربية والإنجليزية</p>
          <textarea id="translation-input" placeholder="اكتب النص للترجمة..." rows="3" class="w-full p-3 border rounded mb-4"></textarea>
          <div class="flex gap-2 mb-4">
            <button class="flex-1 p-3 border rounded hover:bg-gray-50">عربي → إنجليزي</button>
            <button class="flex-1 p-3 border rounded hover:bg-gray-50">إنجليزي → عربي</button>
          </div>
          <button class="w-full p-3 bg-blue-600 text-white rounded hover:bg-blue-700">ترجم</button>
        </div>
      </div>
    </div>
  </div>

  <!-- Summary Dialog -->
  <div id="summary-dialog" class="fixed inset-0 bg-black bg-opacity-50 hidden z-50">
    <div class="flex items-center justify-center min-h-screen p-4">
      <div class="bg-white rounded-lg max-w-2xl w-full">
        <div class="flex items-center justify-between p-6 border-b">
          <h2 class="text-xl font-semibold">ملخص الدرس</h2>
          <button onclick="closeDialog('summary')" class="text-gray-400 hover:text-gray-600">
            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
            </svg>
          </button>
        </div>
        <div class="p-6">
          <p class="text-gray-600 mb-4">احصل على ملخص شامل للدرس</p>
          <button onclick="generateSummary()" class="w-full p-3 bg-blue-600 text-white rounded hover:bg-blue-700 mb-4">إنشاء ملخص</button>
          <div class="p-4 bg-gray-100 rounded">
            <h4 class="font-semibold mb-2">ملخص الدرس:</h4>
            <p id="summary-content">سيتم إنشاء ملخص شامل للدرس باستخدام الذكاء الاصطناعي...</p>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- AI Tutor Dialog -->
  <div id="tutor-dialog" class="fixed inset-0 bg-black bg-opacity-50 hidden z-50">
    <div class="flex items-center justify-center min-h-screen p-4">
      <div class="bg-white rounded-lg max-w-4xl w-full max-h-[80vh] overflow-y-auto">
        <div class="flex items-center justify-between p-6 border-b">
          <h2 class="text-xl font-semibold">AI Lesson Tutor</h2>
          <button onclick="closeDialog('tutor')" class="text-gray-400 hover:text-gray-600">
            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
            </svg>
          </button>
        </div>
        <div class="p-6">
          <p class="text-gray-600 mb-4">Ask me anything about this lesson!</p>
          <div class="space-y-4">
            <textarea id="tutor-question" placeholder="Ask your question..." rows="3" class="w-full p-3 border rounded"></textarea>
            <button onclick="askTutor()" class="px-4 py-3 bg-blue-600 text-white rounded hover:bg-blue-700">Ask Tutor</button>
            <div id="tutor-response" class="p-4 bg-gray-100 rounded hidden">
              <h4 class="font-semibold mb-2">Tutor Response:</h4>
              <p id="tutor-response-text"></p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</Layout>

<script>
  // Dialog management
  function openDialog(dialogName) {
    document.getElementById(`${dialogName}-dialog`).classList.remove('hidden');
  }

  function closeDialog(dialogName) {
    document.getElementById(`${dialogName}-dialog`).classList.add('hidden');
  }

  // Exercise answer checking
  function checkAnswer(button, correctAnswer) {
    const radioButtons = button.parentElement.querySelectorAll('input[type="radio"]');
    let selectedAnswer = '';
    
    radioButtons.forEach(radio => {
      if (radio.checked) {
        selectedAnswer = radio.value;
      }
    });
    
    if (!selectedAnswer) {
      alert('Please select an answer first!');
      return;
    }
    
    if (selectedAnswer === correctAnswer) {
      button.textContent = 'Correct! ✓';
      button.className = 'mt-3 px-4 py-2 bg-green-600 text-white rounded';
      button.disabled = true;
    } else {
      button.textContent = 'Wrong! Try Again';
      button.className = 'mt-3 px-4 py-2 bg-red-600 text-white rounded';
      setTimeout(() => {
        button.textContent = 'Check Answer';
        button.className = 'mt-3 px-4 py-2 bg-blue-600 text-white rounded hover:bg-blue-700';
      }, 2000);
    }
  }

  // TTS functionality
  function speakText(text, language) {
    if ('speechSynthesis' in window) {
      const utterance = new SpeechSynthesisUtterance(text);
      utterance.lang = language === 'en' ? 'en-US' : 'ar-SA';
      utterance.rate = 0.8;
      utterance.pitch = 1;
      speechSynthesis.speak(utterance);
    } else {
      alert('Speech synthesis not supported in this browser');
    }
  }

  // STT functionality
  function startListening() {
    if (!('webkitSpeechRecognition' in window) && !('SpeechRecognition' in window)) {
      alert('Speech recognition not supported in this browser');
      return;
    }

    const button = document.getElementById('stt-button');
    const resultsDiv = document.getElementById('transcribed-text');
    const resultText = document.getElementById('stt-result');
    
    button.textContent = '🎤 Listening...';
    button.disabled = true;
    resultsDiv.classList.add('hidden');

    const SpeechRecognition = window.SpeechRecognition || window.webkitSpeechRecognition;
    const recognition = new SpeechRecognition();
    
    recognition.lang = 'en-US';
    recognition.continuous = false;
    recognition.interimResults = false;

    recognition.onresult = (event) => {
      const transcript = event.results[0][0].transcript;
      resultText.textContent = transcript;
      resultsDiv.classList.remove('hidden');
    };

    recognition.onerror = (event) => {
      console.error('STT error:', event.error);
      alert('Speech recognition error: ' + event.error);
    };

    recognition.onend = () => {
      button.textContent = '🎤 ابدأ التحدث';
      button.disabled = false;
    };

    recognition.start();
  }

  // AI Question functionality
  function askAI() {
    const question = document.getElementById('ai-question-input').value;
    const button = document.getElementById('ai-ask-button');
    const responseDiv = document.getElementById('ai-response');
    const responseText = document.getElementById('ai-response-text');
    
    if (!question.trim()) {
      alert('Please enter a question');
      return;
    }
    
    button.textContent = 'Generating...';
    button.disabled = true;
    
    // Simulate AI response (replace with real AI call)
    setTimeout(() => {
      responseText.textContent = `This is a simulated AI response to: "${question}". In a real implementation, this would call your RAG backend with Cloudflare AI models.`;
      responseDiv.classList.remove('hidden');
      button.textContent = 'اسأل الذكاء الاصطناعي';
      button.disabled = false;
    }, 2000);
  }

  // RAG Search functionality
  function searchRAG() {
    const query = document.getElementById('rag-search-input').value;
    const resultsDiv = document.getElementById('rag-results');
    const resultsContent = document.getElementById('rag-results-content');
    
    if (!query.trim()) {
      alert('Please enter a search query');
      return;
    }
    
    // Simulate RAG search (replace with real RAG backend call)
    resultsContent.innerHTML = `
      <div class="border-b p-3">
        <div class="flex justify-between items-start mb-2">
          <span class="text-sm font-medium bg-blue-100 px-2 py-1 rounded">ai_response</span>
          <span class="text-xs text-gray-500">${new Date().toLocaleDateString()}</span>
        </div>
        <p class="text-sm mb-1"><strong>Content:</strong> Simulated RAG search result for: "${query}"</p>
        <p class="text-xs text-gray-500">Similarity: 95.2%</p>
      </div>
    `;
    
    resultsDiv.classList.remove('hidden');
  }

  // Summary generation
  function generateSummary() {
    const content = document.getElementById('summary-content');
    content.textContent = 'Generating lesson summary using AI... This would call your Cloudflare AI models through the RAG backend to create a comprehensive summary of the lesson content.';
  }

  // AI Tutor functionality
  function askTutor() {
    const question = document.getElementById('tutor-question').value;
    const responseDiv = document.getElementById('tutor-response');
    const responseText = document.getElementById('tutor-response-text');
    
    if (!question.trim()) {
      alert('Please enter a question');
      return;
    }
    
    responseText.textContent = `Tutor response to: "${question}". This would use your RAG backend with Cloudflare AI models to provide personalized tutoring assistance.`;
    responseDiv.classList.remove('hidden');
  }

  // Close dialogs when clicking outside
  document.addEventListener('click', function(event) {
    if (event.target.classList.contains('fixed')) {
      event.target.classList.add('hidden');
    }
  });
</script>
